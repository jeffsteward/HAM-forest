const TriggerTypes = {
    FADE_OUT_AND_IN: 'fade-out-and-in',
    FADE_OUT: 'fade-out',
    FADE_IN: 'fade-in'
};

const MessageEvents = {
    JOIN_GAME: 'join-game',
    TAKE_ACTION: 'take-action'
};

const GameViews = {
    BOARD: 'board',
    WINDOW: 'window'
};